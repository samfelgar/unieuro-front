import React from 'react'
import { BrowserRouter, Switch } from 'react-router-dom'

const Routes = () => {
    return (
        <BrowserRouter>
            <Switch>
                
            </Switch>
        </BrowserRouter>
    )
}

export default Routes